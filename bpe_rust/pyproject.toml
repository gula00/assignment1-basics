[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "bpe_rust"
version = "0.1.0"
description = "Fast BPE training implementation in Rust"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]
